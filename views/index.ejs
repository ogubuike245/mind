<html>
  <%- include('./partials/head.ejs') %>
  <body>
    <%- include('./partials/navbar.ejs') %>
    <h1>WELCOME TO GUBALT SPIKES</h1>

    <% if (locals.user) { %>
    <h1>SELECTED COURSES</h1>

    <% for (const course of locals.selectedCourses) { %>

    <div>
      <button>
        <a href="/api/v1/course/details/<%= course.code %>"
          ><%= course.title %></a
        >
      </button>
    </div>
    <% } %> <% } %>
  </body>
</html>
